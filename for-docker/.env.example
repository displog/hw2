# PostgreSQL (внутри compose — postgres — сервис)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=marketplace
DB_USER=postgres
DB_PASSWORD=postgres

# Приложение
SECRET_KEY=dev-secret-change-in-production
ORDER_LIMIT_MINUTES=5

# pgAdmin
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=admin
